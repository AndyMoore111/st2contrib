---
name: "webhook"
pack: "vadc"
description: "vADC Web Hook"
enabled: false

trigger:
        type: "core.st2.webhook"
        parameters:
            url: "vadc_hook"

action:
    ref: "core.local"
    parameters:
        cmd: "logger {{trigger.headers}} && logger {{trigger.body}}"
